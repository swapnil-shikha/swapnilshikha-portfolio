import Hero from '@/components/Hero';
import Resume from '@/components/Resume';

const HomePage = () => {
  return (
    <main>
      <Hero />
      <Resume />
    </main>
  );
};

export default HomePage;
